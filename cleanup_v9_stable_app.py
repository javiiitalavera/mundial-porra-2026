#!/usr/bin/env python3
from pathlib import Path

ROOT = Path.cwd()

required = ["package.json", "app", "components", "lib", "data"]
for rel in required:
    if not (ROOT / rel).exists():
        raise FileNotFoundError(f"No encuentro {rel}. Ejecuta este script desde la raíz del proyecto mundial-porra-2026.")

files = {
    "components/BottomNav.tsx": '"use client";\n\nimport Link from "next/link";\nimport { usePathname } from "next/navigation";\n\nconst items = [\n  { href: "/", label: "Clasificación" },\n  { href: "/partidos", label: "Calendario" },\n  { href: "/pronosticos", label: "Quinielas" },\n  { href: "/instalar", label: "Instalar" }\n];\n\nfunction isActive(pathname: string, href: string) {\n  if (href === "/") return pathname === "/";\n  return pathname.startsWith(href);\n}\n\nexport function BottomNav() {\n  const pathname = usePathname();\n\n  return (\n    <nav className="bottom-nav" aria-label="Navegación principal">\n      {items.map((item) => (\n        <Link\n          key={item.href}\n          href={item.href}\n          className={isActive(pathname, item.href) ? "active" : ""}\n        >\n          {item.label}\n        </Link>\n      ))}\n    </nav>\n  );\n}\n',
    "components/UpdateStatus.tsx": 'import { formatUpdatedAt, formatUpdatedTime } from "@/lib/lastUpdated";\n\nexport type UpdateStatusPayload = {\n  updatedAt?: string;\n  error?: string;\n  stale?: boolean;\n};\n\nexport function UpdateStatus({ payload }: { payload: UpdateStatusPayload }) {\n  if (!payload.updatedAt) {\n    return (\n      <div className="update-status neutral">\n        <span aria-hidden="true" />\n        <strong>Esperando actualización</strong>\n      </div>\n    );\n  }\n\n  if (payload.error || payload.stale) {\n    return (\n      <div className="update-status warning">\n        <span aria-hidden="true" />\n        <strong>Últimos datos disponibles</strong>\n        <small>{formatUpdatedTime(payload.updatedAt)}</small>\n      </div>\n    );\n  }\n\n  return (\n    <div className="update-status ok">\n      <span aria-hidden="true" />\n      <strong>{formatUpdatedAt(payload.updatedAt)}</strong>\n    </div>\n  );\n}\n',
    "components/StandingCard.tsx": 'import Link from "next/link";\nimport type { Standing } from "@/lib/types";\nimport { normalizePlayerForUrl } from "@/lib/format";\n\nconst medals = ["🥇", "🥈", "🥉"];\n\nexport function StandingCard({\n  row,\n  position\n}: {\n  row: Standing;\n  position: number;\n  compact?: boolean;\n}) {\n  const subtitle =\n    row.played === 0\n      ? "Sin partidos puntuados"\n      : `${row.correct}/${row.played} aciertos · ${row.percentage}%`;\n\n  return (\n    <Link\n      href={`/pronosticos/${normalizePlayerForUrl(row.player)}`}\n      className="standing-row"\n    >\n      <div className="standing-position">\n        {medals[position - 1] ?? position}\n      </div>\n\n      <div className="standing-person">\n        <div className="standing-name">{row.player}</div>\n        <div className="muted">{subtitle}</div>\n      </div>\n\n      <div className="standing-points">\n        <strong>{row.points}</strong>\n        <span>pts</span>\n      </div>\n    </Link>\n  );\n}\n',
    "components/MatchCard.tsx": 'import { formatMatchDate, formatSpainTimeFromEt, resultLabel } from "@/lib/format";\nimport { getMatchPredictions, signFromResult } from "@/lib/scoring";\nimport type { Match, MatchResult, Pick } from "@/lib/types";\n\nfunction statusLabel(result?: MatchResult): string {\n  if (!result) return "Pendiente";\n  if (result.status === "LIVE") return result.minute ? `En juego · ${result.minute}\'` : "En juego";\n  if (result.status === "FINISHED") return "Finalizado";\n  return "Pendiente";\n}\n\nexport function MatchCard({\n  match,\n  result\n}: {\n  match: Match;\n  result?: MatchResult;\n  featured?: boolean;\n}) {\n  const actual = signFromResult(result);\n  const predictions = getMatchPredictions(match.id);\n\n  const grouped = predictions.reduce(\n    (acc, item) => {\n      acc[item.pick].push(item.player);\n      return acc;\n    },\n    { "1": [], X: [], "2": [] } as Record<Pick, string[]>\n  );\n\n  const renderPick = (pick: Pick) => {\n    const names = grouped[pick];\n    const isHit = actual === pick && result?.status === "FINISHED";\n\n    return (\n      <details className="pick-details">\n        <summary className={isHit ? "pick-button is-hit" : "pick-button"}>\n          <span>{pick}</span>\n          <strong>{names.length}</strong>\n        </summary>\n        <div className="pick-panel">\n          <div className="pick-panel-title">Han apostado por el {pick}</div>\n          <div className="pick-names">{names.length ? names.join(", ") : "Nadie"}</div>\n        </div>\n      </details>\n    );\n  };\n\n  return (\n    <article className="match-card">\n      <div className="match-topline">\n        <div className="match-kicker">\n          <span>{formatMatchDate(match.date)}</span>\n          <span>Grupo {match.group ?? "—"}</span>\n        </div>\n        <span className={result?.status === "FINISHED" ? "match-state is-done" : "match-state"}>\n          {statusLabel(result)}\n        </span>\n      </div>\n\n      <div className="match-main">\n        <div className="team home">{match.home}</div>\n        <div className={result ? "score is-done" : "score"}>{resultLabel(result)}</div>\n        <div className="team away">{match.away}</div>\n      </div>\n\n      <div className="match-time">\n        <span>{match.timeEt ? formatSpainTimeFromEt(match.timeEt) : "Hora pendiente"}</span>\n        {match.timeLocal ? <span>{match.timeLocal} local</span> : null}\n        {match.city ? <span>{match.city}</span> : null}\n      </div>\n\n      <div className="pick-row">\n        {renderPick("1")}\n        {renderPick("X")}\n        {renderPick("2")}\n      </div>\n    </article>\n  );\n}\n',
    "lib/lastUpdated.ts": 'export function formatUpdatedTime(updatedAt?: string): string {\n  if (!updatedAt) return "";\n\n  const date = new Date(updatedAt);\n  if (Number.isNaN(date.getTime())) return "";\n\n  return new Intl.DateTimeFormat("es-ES", {\n    hour: "2-digit",\n    minute: "2-digit",\n    timeZone: "Europe/Madrid"\n  }).format(date);\n}\n\nexport function formatUpdatedAt(updatedAt?: string): string {\n  const time = formatUpdatedTime(updatedAt);\n  return time ? `Actualizado ${time}` : "Actualizando";\n}\n',
    "app/page.tsx": 'import { StandingCard } from "@/components/StandingCard";\nimport { UpdateStatus } from "@/components/UpdateStatus";\nimport { formatDateSection, resultLabel } from "@/lib/format";\nimport { getFootballDataResults } from "@/lib/footballData";\nimport { getMatches, getStandings } from "@/lib/scoring";\n\nexport const revalidate = 600;\n\ntype ResultsPayload = Awaited<ReturnType<typeof getFootballDataResults>>;\n\nfunction getLastFinishedMatch(results: ResultsPayload["results"]) {\n  return getMatches()\n    .filter((match) => results[match.id]?.status === "FINISHED")\n    .sort((a, b) => {\n      const dateA = `${a.date ?? ""}-${String(a.order).padStart(3, "0")}`;\n      const dateB = `${b.date ?? ""}-${String(b.order).padStart(3, "0")}`;\n      return dateB.localeCompare(dateA);\n    })[0];\n}\n\nexport default async function HomePage() {\n  const payload = await getFootballDataResults();\n  const standings = getStandings(payload.results);\n  const played = Object.keys(payload.results).length;\n  const total = getMatches().length;\n  const lastMatch = getLastFinishedMatch(payload.results);\n  const lastResult = lastMatch ? payload.results[lastMatch.id] : undefined;\n\n  return (\n    <section className="screen">\n      <header className="page-header">\n        <div className="section-label">🏆 Mundial 2026</div>\n        <h1>Clasificación</h1>\n        <p>{played}/{total} partidos puntuados</p>\n        <UpdateStatus payload={payload} />\n      </header>\n\n      {lastMatch && lastResult ? (\n        <section className="last-result-card">\n          <div>\n            <span className="section-label">Último resultado</span>\n            <h2>{lastMatch.home} - {lastMatch.away}</h2>\n            <p>{formatDateSection(lastMatch.date)}</p>\n          </div>\n          <strong>{resultLabel(lastResult)}</strong>\n        </section>\n      ) : null}\n\n      <section className="ranking-list">\n        {standings.map((row, index) => (\n          <StandingCard key={row.player} row={row} position={index + 1} />\n        ))}\n      </section>\n    </section>\n  );\n}\n',
    "app/partidos/page.tsx": 'import { MatchCard } from "@/components/MatchCard";\nimport { UpdateStatus } from "@/components/UpdateStatus";\nimport { formatDateSection, formatLongMatchDate } from "@/lib/format";\nimport { getFootballDataResults } from "@/lib/footballData";\nimport { getMatches, getNextMatch } from "@/lib/scoring";\n\nexport const revalidate = 600;\n\nexport default async function MatchesPage() {\n  const matches = getMatches();\n  const payload = await getFootballDataResults();\n  const played = Object.keys(payload.results).length;\n  const nextMatch = getNextMatch(payload.results);\n\n  const grouped = matches.reduce((acc, match) => {\n    const key = match.date ?? "Fecha pendiente";\n    if (!acc[key]) acc[key] = [];\n    acc[key].push(match);\n    return acc;\n  }, {} as Record<string, typeof matches>);\n\n  return (\n    <section className="screen">\n      <header className="page-header">\n        <div className="section-label">Calendario</div>\n        <h1>Calendario</h1>\n        <p>{matches.length} partidos · {played} puntuados</p>\n\n        <div className="header-actions">\n          <UpdateStatus payload={payload} />\n          {nextMatch ? (\n            <a className="jump-button" href={`#partido-${nextMatch.id}`}>\n              Ver hoy\n            </a>\n          ) : null}\n        </div>\n      </header>\n\n      <div className="date-stack">\n        {Object.entries(grouped).map(([date, dayMatches]) => (\n          <section key={date} className="date-section">\n            <div className="date-title">\n              <h2>{formatDateSection(date)}</h2>\n              <span>{formatLongMatchDate(date)}</span>\n            </div>\n\n            <div className="match-stack">\n              {dayMatches.map((match) => (\n                <div\n                  key={match.id}\n                  id={`partido-${match.id}`}\n                  className={nextMatch?.id === match.id ? "match-anchor next-match-anchor" : "match-anchor"}\n                >\n                  <MatchCard match={match} result={payload.results[match.id]} />\n                </div>\n              ))}\n            </div>\n          </section>\n        ))}\n      </div>\n    </section>\n  );\n}\n',
    "app/pronosticos/page.tsx": 'import Link from "next/link";\nimport { UpdateStatus } from "@/components/UpdateStatus";\nimport { normalizePlayerForUrl } from "@/lib/format";\nimport { getFootballDataResults } from "@/lib/footballData";\nimport { getPlayerSummaries } from "@/lib/scoring";\n\nexport const revalidate = 600;\n\nexport default async function PredictionsPage() {\n  const payload = await getFootballDataResults();\n  const players = getPlayerSummaries(payload.results);\n\n  return (\n    <section className="screen">\n      <header className="page-header">\n        <div className="section-label">{players.length} participantes</div>\n        <h1>Quinielas</h1>\n        <p>Consulta la quiniela de cada participante.</p>\n        <UpdateStatus payload={payload} />\n      </header>\n\n      <div className="player-list">\n        {players.map((row) => (\n          <Link\n            key={row.player}\n            href={`/pronosticos/${normalizePlayerForUrl(row.player)}`}\n            className="player-row"\n          >\n            <div className="avatar">{row.player.slice(0, 1)}</div>\n            <div className="player-info">\n              <div className="player-name">{row.player}</div>\n              <div className="muted">\n                {row.played === 0 ? "Ver quiniela" : `${row.correct} aciertos · ${row.wrong} fallos`}\n              </div>\n            </div>\n            <div className="row-arrow" aria-hidden="true">›</div>\n          </Link>\n        ))}\n      </div>\n    </section>\n  );\n}\n',
    "app/pronosticos/[player]/page.tsx": 'import Link from "next/link";\nimport { notFound } from "next/navigation";\nimport { formatDateSection, resultLabel } from "@/lib/format";\nimport { getFootballDataResults } from "@/lib/footballData";\nimport { getPlayerPredictions, getPlayerSummary } from "@/lib/scoring";\n\nexport const revalidate = 600;\n\nexport default async function PlayerPredictionPage({\n  params\n}: {\n  params: Promise<{ player: string }>;\n}) {\n  const { player: rawPlayer } = await params;\n  const player = decodeURIComponent(rawPlayer);\n\n  const payload = await getFootballDataResults();\n  const predictions = getPlayerPredictions(player, payload.results);\n  if (predictions.length === 0) notFound();\n\n  const summary = getPlayerSummary(player, payload.results);\n\n  const grouped = predictions.reduce((acc, item) => {\n    const key = item.match.date ?? "Fecha pendiente";\n    if (!acc[key]) acc[key] = [];\n    acc[key].push(item);\n    return acc;\n  }, {} as Record<string, typeof predictions>);\n\n  return (\n    <section className="screen">\n      <header className="page-header">\n        <Link href="/pronosticos" className="back-link">← Quinielas</Link>\n        <div className="section-label">Quiniela de</div>\n        <h1>{player}</h1>\n        <p>\n          {summary.played === 0\n            ? "72 pronósticos registrados"\n            : `${summary.correct} aciertos · ${summary.wrong} fallos · ${summary.pending} pendientes`}\n        </p>\n      </header>\n\n      <div className="date-stack">\n        {Object.entries(grouped).map(([date, items]) => (\n          <section key={date} className="date-section">\n            <div className="date-title slim">\n              <h2>{formatDateSection(date)}</h2>\n            </div>\n\n            <div className="prediction-stack">\n              {items.map((item) => (\n                <article key={item.matchId} className="prediction-row">\n                  <div>\n                    <div className="fixture-name">{item.match.label}</div>\n                    <div className="muted">Grupo {item.match.group ?? "—"}</div>\n                  </div>\n\n                  <div className="prediction-right">\n                    <span className="pick-badge">{item.pick}</span>\n                    {item.isCorrect === null ? null : item.isCorrect ? (\n                      <span className="result-pill ok">+1</span>\n                    ) : (\n                      <span className="result-pill ko">0</span>\n                    )}\n                    {item.actual ? (\n                      <span className="muted score-mini">{resultLabel(payload.results[item.matchId])}</span>\n                    ) : null}\n                  </div>\n                </article>\n              ))}\n            </div>\n          </section>\n        ))}\n      </div>\n    </section>\n  );\n}\n',
    "app/instalar/page.tsx": 'export default function InstallPage() {\n  return (\n    <section className="screen">\n      <header className="page-header">\n        <div className="section-label">App móvil</div>\n        <h1>Instalar</h1>\n        <p>Añade Mundial 2026 a la pantalla de inicio y ábrela como una app.</p>\n      </header>\n\n      <div className="install-hero-card">\n        <img src="/icon-192.png" alt="Icono Mundial 2026" className="install-icon" />\n        <div>\n          <h2>Mundial 2026</h2>\n          <p>Clasificación, calendario y quinielas siempre a mano.</p>\n        </div>\n      </div>\n\n      <section className="install-section">\n        <h2>iPhone / iPad</h2>\n        <ol className="install-steps">\n          <li>Abre esta web en Safari.</li>\n          <li>Toca compartir.</li>\n          <li>Elige “Añadir a pantalla de inicio”.</li>\n          <li>Confirma con “Añadir”.</li>\n        </ol>\n      </section>\n\n      <section className="install-section">\n        <h2>Android</h2>\n        <ol className="install-steps">\n          <li>Abre esta web en Chrome.</li>\n          <li>Toca el menú de tres puntos.</li>\n          <li>Elige “Instalar aplicación” o “Añadir a pantalla de inicio”.</li>\n          <li>Confirma.</li>\n        </ol>\n      </section>\n    </section>\n  );\n}\n',
    "app/api/results/route.ts": 'import { NextResponse } from "next/server";\nimport { getFootballDataResults } from "@/lib/footballData";\n\nexport const revalidate = 600;\n\nexport async function GET() {\n  const payload = await getFootballDataResults();\n\n  return NextResponse.json(payload, {\n    headers: {\n      "Cache-Control": "s-maxage=600, stale-while-revalidate=1800"\n    }\n  });\n}\n',
    "app/globals.css": ':root {\n  --bg: #fff4d8;\n  --surface: rgba(255, 255, 255, 0.9);\n  --surface-strong: #fffdf8;\n  --text: #17130c;\n  --muted: #7d705f;\n  --line: rgba(24, 18, 9, 0.12);\n  --accent: #ffb21a;\n  --accent-soft: #fff0c2;\n  --green: #138a55;\n  --green-soft: #e8f6ef;\n  --amber: #946400;\n  --red: #b94a3a;\n  --shadow: 0 8px 24px rgba(58, 42, 13, 0.06);\n}\n\n* {\n  box-sizing: border-box;\n}\n\nhtml {\n  min-height: 100%;\n  scroll-behavior: smooth;\n  background: var(--bg);\n}\n\nbody {\n  min-height: 100%;\n  margin: 0;\n  background: radial-gradient(circle at 50% -10%, rgba(255, 205, 82, 0.22), transparent 38%), var(--bg);\n  color: var(--text);\n  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", Roboto, Arial, sans-serif;\n  -webkit-font-smoothing: antialiased;\n}\n\na {\n  color: inherit;\n}\n\nbutton,\nsummary {\n  font-family: inherit;\n}\n\n.screen {\n  width: 100%;\n  max-width: 520px;\n  margin: 0 auto;\n  padding: 16px 14px 96px;\n}\n\n.page-header,\n.app-header {\n  padding: 6px 0 14px;\n}\n\n.section-label {\n  color: var(--amber);\n  font-size: 12px;\n  font-weight: 850;\n  letter-spacing: 0.045em;\n  line-height: 1.2;\n  text-transform: uppercase;\n}\n\n.page-header h1,\n.app-header h1 {\n  margin: 6px 0 0;\n  color: var(--text);\n  font-size: clamp(38px, 10vw, 50px);\n  font-weight: 850;\n  letter-spacing: -0.055em;\n  line-height: 0.98;\n}\n\n.page-header p,\n.app-header p {\n  margin: 8px 0 0;\n  color: var(--muted);\n  font-size: 16px;\n  font-weight: 650;\n  line-height: 1.3;\n}\n\n.back-link {\n  display: inline-flex;\n  margin-bottom: 10px;\n  color: var(--muted);\n  font-size: 14px;\n  font-weight: 750;\n  text-decoration: none;\n}\n\n.update-status {\n  display: inline-flex;\n  align-items: center;\n  gap: 7px;\n  width: fit-content;\n  max-width: 100%;\n  margin-top: 10px;\n  padding: 7px 10px;\n  border: 1px solid var(--line);\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.7);\n  color: var(--text);\n  font-size: 13px;\n  font-weight: 800;\n  line-height: 1;\n}\n\n.update-status span {\n  width: 8px;\n  height: 8px;\n  flex: 0 0 auto;\n  border-radius: 999px;\n  background: var(--muted);\n}\n\n.update-status small {\n  color: var(--muted);\n  font-size: 12px;\n  font-weight: 750;\n}\n\n.update-status.ok {\n  border-color: rgba(19, 138, 85, 0.18);\n  background: var(--green-soft);\n  color: var(--green);\n}\n\n.update-status.ok span {\n  background: var(--green);\n}\n\n.update-status.warning {\n  border-color: rgba(148, 100, 0, 0.20);\n  background: var(--accent-soft);\n  color: var(--amber);\n}\n\n.update-status.warning span {\n  background: var(--amber);\n}\n\n.update-status.neutral {\n  color: var(--muted);\n}\n\n.ranking-list,\n.player-list,\n.match-stack,\n.prediction-stack,\n.date-stack {\n  display: grid;\n  gap: 8px;\n}\n\n.ranking-list {\n  margin-top: 10px;\n}\n\n.standing-row {\n  display: grid;\n  grid-template-columns: 42px minmax(0, 1fr) auto;\n  align-items: center;\n  gap: 10px;\n  min-height: 58px;\n  padding: 10px 12px;\n  border: 1px solid var(--line);\n  border-radius: 18px;\n  background: var(--surface);\n  box-shadow: var(--shadow);\n  text-decoration: none;\n}\n\n.standing-position {\n  width: 32px;\n  color: var(--muted);\n  font-size: 16px;\n  font-weight: 850;\n  text-align: center;\n}\n\n.standing-person {\n  min-width: 0;\n}\n\n.standing-name {\n  overflow: hidden;\n  color: var(--text);\n  font-size: 18px;\n  font-weight: 850;\n  letter-spacing: -0.025em;\n  line-height: 1.05;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.muted {\n  color: var(--muted);\n  font-size: 13px;\n  font-weight: 500;\n  line-height: 1.15;\n}\n\n.standing-points {\n  display: grid;\n  min-width: 40px;\n  justify-items: end;\n}\n\n.standing-points strong {\n  color: var(--text);\n  font-size: 30px;\n  font-weight: 850;\n  letter-spacing: -0.045em;\n  line-height: 0.9;\n}\n\n.standing-points span {\n  color: var(--muted);\n  font-size: 10px;\n  font-weight: 850;\n  letter-spacing: 0.08em;\n  line-height: 1;\n  text-transform: uppercase;\n}\n\n.last-result-card {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n  margin: 2px 0 10px;\n  padding: 12px 14px;\n  border: 1px solid var(--line);\n  border-radius: 18px;\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.last-result-card h2 {\n  margin: 4px 0 2px;\n  color: var(--text);\n  font-size: 21px;\n  font-weight: 850;\n  letter-spacing: -0.035em;\n  line-height: 1.05;\n}\n\n.last-result-card p {\n  margin: 0;\n  color: var(--muted);\n  font-size: 13px;\n}\n\n.last-result-card strong {\n  flex: 0 0 auto;\n  padding: 9px 11px;\n  border-radius: 14px;\n  background: var(--text);\n  color: #fff7df;\n  font-size: 20px;\n  font-weight: 850;\n  letter-spacing: -0.04em;\n  line-height: 1;\n}\n\n.header-actions {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n  margin-top: 12px;\n}\n\n.header-actions .update-status {\n  margin-top: 0;\n}\n\n.jump-button {\n  display: inline-flex;\n  min-height: 40px;\n  flex: 0 0 auto;\n  align-items: center;\n  justify-content: center;\n  margin-left: auto;\n  padding: 0 16px;\n  border: 1px solid rgba(24, 18, 9, 0.12);\n  border-radius: 999px;\n  background: var(--accent);\n  box-shadow: var(--shadow);\n  color: var(--text);\n  font-size: 15px;\n  font-weight: 850;\n  text-decoration: none;\n}\n\n.date-section {\n  margin-top: 20px;\n}\n\n.date-section:first-child {\n  margin-top: 0;\n}\n\n.date-title {\n  display: flex;\n  align-items: flex-end;\n  justify-content: space-between;\n  gap: 14px;\n  margin-bottom: 10px;\n}\n\n.date-title h2 {\n  margin: 0;\n  color: var(--text);\n  font-size: 28px;\n  font-weight: 850;\n  letter-spacing: -0.045em;\n  line-height: 1.05;\n}\n\n.date-title span {\n  color: var(--muted);\n  font-size: 15px;\n  font-weight: 500;\n  text-align: right;\n}\n\n.match-anchor {\n  scroll-margin-top: 18px;\n}\n\n.next-match-anchor .match-card {\n  border-color: rgba(255, 178, 26, 0.75);\n}\n\n.match-card {\n  padding: 14px;\n  border: 1px solid var(--line);\n  border-radius: 20px;\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.match-topline {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n  margin-bottom: 14px;\n}\n\n.match-kicker {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 6px;\n}\n\n.match-kicker span,\n.match-state {\n  display: inline-flex;\n  min-height: 30px;\n  align-items: center;\n  padding: 0 10px;\n  border: 1px solid rgba(24, 18, 9, 0.10);\n  border-radius: 999px;\n  background: rgba(255, 250, 240, 0.72);\n  color: var(--muted);\n  font-size: 13px;\n  font-weight: 750;\n}\n\n.match-state.is-done {\n  border-color: rgba(19, 138, 85, 0.18);\n  background: var(--green-soft);\n  color: var(--green);\n}\n\n.match-main {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);\n  align-items: center;\n  gap: 10px;\n  margin-bottom: 10px;\n}\n\n.team {\n  min-width: 0;\n  color: var(--text);\n  font-size: 24px;\n  font-weight: 850;\n  letter-spacing: -0.035em;\n  line-height: 1.05;\n}\n\n.team.away {\n  text-align: right;\n}\n\n.score {\n  display: grid;\n  min-width: 48px;\n  min-height: 42px;\n  place-items: center;\n  padding: 8px 10px;\n  border: 1px solid rgba(24, 18, 9, 0.10);\n  border-radius: 14px;\n  background: rgba(255, 245, 224, 0.82);\n  color: var(--muted);\n  font-size: 20px;\n  font-weight: 850;\n  letter-spacing: -0.04em;\n}\n\n.score.is-done {\n  background: var(--accent);\n  color: var(--text);\n}\n\n.match-time {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 7px;\n  margin-bottom: 12px;\n  color: var(--muted);\n  font-size: 14px;\n  line-height: 1.3;\n}\n\n.match-time span:first-child {\n  color: var(--text);\n  font-weight: 800;\n}\n\n.pick-row {\n  display: grid;\n  gap: 7px;\n}\n\n.pick-details {\n  position: relative;\n}\n\n.pick-details summary {\n  list-style: none;\n}\n\n.pick-details summary::-webkit-details-marker {\n  display: none;\n}\n\n.pick-button {\n  display: flex;\n  min-height: 40px;\n  align-items: center;\n  justify-content: space-between;\n  padding: 8px 12px;\n  border: 1px solid var(--line);\n  border-radius: 16px;\n  background: rgba(255, 250, 240, 0.72);\n  cursor: pointer;\n  font-size: 17px;\n  font-weight: 850;\n}\n\n.pick-button.is-hit {\n  border-color: rgba(255, 178, 26, 0.42);\n  background: var(--accent-soft);\n}\n\n.pick-panel {\n  margin-top: 6px;\n  padding: 10px 12px;\n  border: 1px solid var(--line);\n  border-radius: 14px;\n  background: var(--surface-strong);\n}\n\n.pick-panel-title {\n  margin-bottom: 4px;\n  color: var(--muted);\n  font-size: 12px;\n  font-weight: 800;\n}\n\n.pick-names {\n  color: var(--text);\n  font-size: 13px;\n  line-height: 1.35;\n}\n\n.player-row {\n  display: grid;\n  grid-template-columns: 42px minmax(0, 1fr) auto;\n  align-items: center;\n  gap: 10px;\n  min-height: 58px;\n  padding: 10px 12px;\n  border: 1px solid var(--line);\n  border-radius: 18px;\n  background: var(--surface);\n  box-shadow: var(--shadow);\n  text-decoration: none;\n}\n\n.avatar {\n  display: grid;\n  width: 38px;\n  height: 38px;\n  place-items: center;\n  border-radius: 14px;\n  background: var(--accent-soft);\n  color: var(--amber);\n  font-size: 18px;\n  font-weight: 850;\n}\n\n.player-info {\n  min-width: 0;\n}\n\n.player-name {\n  overflow: hidden;\n  color: var(--text);\n  font-size: 18px;\n  font-weight: 850;\n  letter-spacing: -0.025em;\n  line-height: 1.05;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.row-arrow {\n  color: var(--muted);\n  font-size: 26px;\n  line-height: 1;\n}\n\n.prediction-row {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n  min-height: 58px;\n  padding: 10px 12px;\n  border: 1px solid var(--line);\n  border-radius: 18px;\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.fixture-name {\n  color: var(--text);\n  font-size: 17px;\n  font-weight: 800;\n  letter-spacing: -0.025em;\n}\n\n.prediction-right {\n  display: flex;\n  flex: 0 0 auto;\n  align-items: center;\n  gap: 6px;\n}\n\n.pick-badge,\n.result-pill {\n  display: inline-flex;\n  min-width: 34px;\n  height: 34px;\n  align-items: center;\n  justify-content: center;\n  border-radius: 12px;\n  font-size: 15px;\n  font-weight: 850;\n}\n\n.pick-badge {\n  background: var(--accent-soft);\n  color: var(--text);\n}\n\n.result-pill.ok {\n  background: var(--green-soft);\n  color: var(--green);\n}\n\n.result-pill.ko {\n  background: #f7e5df;\n  color: var(--red);\n}\n\n.score-mini {\n  min-width: 36px;\n  text-align: right;\n}\n\n.install-hero-card,\n.install-section {\n  margin-top: 12px;\n  padding: 14px;\n  border: 1px solid var(--line);\n  border-radius: 20px;\n  background: var(--surface);\n  box-shadow: var(--shadow);\n}\n\n.install-hero-card {\n  display: flex;\n  align-items: center;\n  gap: 12px;\n}\n\n.install-icon {\n  width: 58px;\n  height: 58px;\n  border-radius: 18px;\n}\n\n.install-hero-card h2,\n.install-section h2 {\n  margin: 0 0 4px;\n  color: var(--text);\n  font-size: 22px;\n  font-weight: 850;\n  letter-spacing: -0.035em;\n}\n\n.install-hero-card p {\n  margin: 0;\n  color: var(--muted);\n  font-size: 14px;\n}\n\n.install-steps {\n  margin: 8px 0 0;\n  padding-left: 20px;\n  color: var(--muted);\n  font-size: 15px;\n  line-height: 1.45;\n}\n\n.bottom-nav {\n  position: fixed;\n  left: 50%;\n  bottom: max(10px, env(safe-area-inset-bottom));\n  z-index: 100;\n  display: grid;\n  width: min(496px, calc(100vw - 20px));\n  height: 58px;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: 4px;\n  padding: 5px;\n  transform: translateX(-50%);\n  border: 1px solid var(--line);\n  border-radius: 999px;\n  background: rgba(255, 250, 237, 0.96);\n  box-shadow: 0 8px 28px rgba(50, 35, 9, 0.13);\n  backdrop-filter: blur(18px);\n}\n\n.bottom-nav a {\n  display: flex;\n  min-width: 0;\n  height: 48px;\n  align-items: center;\n  justify-content: center;\n  padding: 0 2px;\n  border-radius: 999px;\n  color: var(--muted);\n  font-size: clamp(11px, 3.05vw, 13px);\n  font-weight: 850;\n  letter-spacing: -0.02em;\n  line-height: 1;\n  text-align: center;\n  text-decoration: none;\n  white-space: nowrap;\n}\n\n.bottom-nav a.active {\n  background: var(--accent);\n  color: var(--text);\n}\n\n@media (max-width: 390px) {\n  .screen {\n    padding-right: 12px;\n    padding-left: 12px;\n  }\n\n  .page-header h1,\n  .app-header h1 {\n    font-size: 38px;\n  }\n\n  .page-header p,\n  .app-header p {\n    font-size: 16px;\n  }\n\n  .team {\n    font-size: 22px;\n  }\n\n  .bottom-nav a {\n    font-size: 11px;\n    letter-spacing: -0.04em;\n  }\n}\n',
    "README.md": '# Mundial 2026 — Porra entre amigos\n\nApp móvil en Next.js para seguir una porra privada del Mundial 2026.\n\n## Qué incluye\n\n- 16 participantes.\n- 72 partidos de fase de grupos.\n- 1.152 pronósticos importados desde el PDF original.\n- Clasificación automática.\n- Calendario completo con salto al partido actual.\n- Quiniela individual por participante.\n- Resultados automáticos mediante football-data.org.\n- Caché de resultados para no depender de la API en cada visita.\n- Soporte opcional de Redis/Upstash para conservar el último dato válido si la API falla.\n\n## Variables de entorno\n\nEn Vercel:\n\n```bash\nFOOTBALL_DATA_TOKEN\n```\n\nOpcional si se usa Redis/Upstash:\n\n```bash\nUPSTASH_REDIS_REST_URL\nUPSTASH_REDIS_REST_TOKEN\n```\n\n## Desarrollo local\n\n```bash\nnpm install\nnpm run dev\n```\n\nAbrir:\n\n```bash\nhttp://localhost:3000\n```\n\n## Build\n\n```bash\nnpm run build\n```\n\n## Deploy\n\n```bash\ngit add .\ngit commit -m "..."\ngit push\nnpx vercel --prod\n```\n\n## Resultados\n\nLa ruta de resultados es:\n\n```bash\n/api/results\n```\n\nDevuelve un payload con:\n\n- `results`: resultados ya disponibles.\n- `updatedAt`: hora de última actualización.\n- `matchedFixtures`: partidos emparejados con la API.\n- `rawMatches`: partidos recibidos desde la API.\n- `cache`: origen del dato.\n- `stale`: si se están usando últimos datos disponibles por fallo temporal.\n\nLa interfaz no muestra conceptos técnicos al usuario. Solo muestra una pastilla de estado: actualizado, últimos datos disponibles o esperando actualización.\n\n## Estructura\n\n```bash\napp/\n  page.tsx                 # Clasificación\n  partidos/page.tsx        # Calendario\n  pronosticos/page.tsx     # Lista de quinielas\n  pronosticos/[player]/    # Quiniela individual\n  instalar/page.tsx        # Instalación móvil\n  api/results/route.ts     # API interna\n\ncomponents/\n  BottomNav.tsx\n  MatchCard.tsx\n  StandingCard.tsx\n  UpdateStatus.tsx\n\nlib/\n  footballData.ts\n  scoring.ts\n  format.ts\n  lastUpdated.ts\n  types.ts\n\ndata/\n  matches.json\n  players.json\n  predictions.json\n```\n\n## Notas\n\nLa API externa puede ir con retraso en el plan gratuito. Para una porra privada es suficiente: la app actualiza de forma conservadora y evita quemar el límite de llamadas.\n',
}

for rel, content in files.items():
    path = ROOT / rel
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content, encoding="utf-8")

# Poda de scripts temporales en raíz. El script actual no coincide con estos patrones.
delete_patterns = [
    "apply_*.py",
    "rollback_*.py",
    "revert_*.py",
    "final_layout_fix.py",
    "*.zip",
]

deleted = []
for pattern in delete_patterns:
    for path in ROOT.glob(pattern):
        if path.is_file() and path.name != Path(__file__).name:
            path.unlink()
            deleted.append(path.name)

# Limpieza de cachés locales si estuvieran versionadas por error.
for rel in [".next", "node_modules"]:
    path = ROOT / rel
    if path.exists():
        print(f"Aviso: {rel} existe localmente. No lo borro desde el script, pero no debería estar versionado.")

print("V9 limpieza profunda aplicada.")
print("")
print("Archivos consolidados:")
for rel in files:
    print(f"- {rel}")
print("")
if deleted:
    print("Scripts/artefactos temporales eliminados:")
    for name in sorted(deleted):
        print(f"- {name}")
else:
    print("No había scripts temporales que borrar en raíz.")
print("")
print("Ahora ejecuta:")
print("npm run build")
print("git status --short")
print("git add .")
print("git commit -m 'Clean up app structure and UI'")
print("git push")
print("npx vercel --prod")
